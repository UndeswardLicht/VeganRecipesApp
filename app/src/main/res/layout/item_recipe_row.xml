<?xml version="1.0" encoding="utf-8"?>
<LinearLayout
    xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="horizontal"
    android:gravity="center_vertical"
    android:paddingHorizontal="12dp"
    android:paddingVertical="10dp"
    android:background="?attr/selectableItemBackground"
    android:clickable="true"
    android:focusable="true">

    <!-- Type emoji for Dish or Drink -->
    <TextView
        android:id="@+id/text_row_type"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textSize="18sp"
        android:gravity="center" />

    <!-- Title takes the remaining space -->
    <TextView
        android:id="@+id/text_row_title"
        android:layout_width="0dp"
        android:layout_height="wrap_content"
        android:layout_weight="1"
        android:layout_marginStart="8dp"
        android:maxLines="1"
        android:ellipsize="end"
        android:textSize="14sp" />

    <!-- Prep time -->
    <TextView
        android:id="@+id/text_row_time"
        android:layout_width="40dp"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#888888"
        android:gravity="end" />

    <!-- Servings -->
    <TextView
        android:id="@+id/text_row_servings"
        android:layout_width="32dp"
        android:layout_height="wrap_content"
        android:textSize="12sp"
        android:textColor="#888888"
        android:gravity="end" />

    <Button
        android:id="@+id/button_row_delete"
        style="?attr/borderlessButtonStyle"
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:minWidth="0dp"
        android:padding="4dp"
        android:text="@string/delete"
        android:textColor="#CC0000"
        android:textSize="11sp" />

</LinearLayout>
